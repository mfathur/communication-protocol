const config = {
    HOST_NAME: "localhost",
    PORT: 3004,
    SOCKET_SERVER_HOST_NAME: "localhost",
    SOCKET_SERVER_PORT: 3002,
    BROKER_HOST_NAME: "localhost",
    BROKER_PORT: 1883
}

module.exports = config;